<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ——— базовые координаты вашего проекта ——— -->
    <groupId>org.randomfetcher</groupId>
    <artifactId>RandomFetcher</artifactId>
    <version>1.3.1-SNAPSHOT</version>

    <properties>
        <java.version>21</java.version>

        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.compiler.enablePreview>true</maven.compiler.enablePreview>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- версии сторонних библиотек -->
        <sqlite.jdbc.version>3.46.0.0</sqlite.jdbc.version>
        <jackson.version>2.17.0</jackson.version>
        <slf4j.version>2.0.13</slf4j.version>

        <junit-jupiter.version>5.13.1</junit-jupiter.version>
        <mockito-junit-jupiter.version>5.17.0</mockito-junit-jupiter.version>
        <jetbrains.version>24.0.1</jetbrains.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.github.cdimascio</groupId>
            <artifactId>java-dotenv</artifactId>
            <version>5.2.2</version>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-inline</artifactId>
            <version>5.2.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-swing-junit</artifactId>
            <version>3.17.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.github.tomakehurst</groupId>
            <artifactId>wiremock</artifactId>
            <version>3.0.1</version>
            <type>pom</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.6.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-devtools-v129</artifactId>
            <version>4.27.0</version>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.20.1</version>
        </dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>4.33.0</version>
        </dependency>
        <dependency>
            <groupId>org.jfree</groupId>
            <artifactId>jfreechart</artifactId>
            <version>1.5.5</version>
        </dependency>
        <dependency>
            <groupId>org.jfree</groupId>
            <artifactId>jcommon</artifactId>
            <version>1.0.24</version>
        </dependency>
        <dependency>
            <groupId>net.java.dev.jna</groupId>
            <artifactId>jna</artifactId>
            <version>5.14.0</version>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.46.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <!-- logback-classic уже тянет slf4j-api, но добавим напрямую,
             чтобы IDE не показывала «provided» -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.5.18</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito-junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.2</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>net.bytebuddy</groupId>
                    <artifactId>byte-buddy</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>${jetbrains.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
    <profiles>
        <!-- mvn verify -DincludeUi=true -->
        <profile>
            <id>include-ui</id>
            <activation>
                <property>
                    <name>includeUi</name>
                    <value>true</value>
                </property>
            </activation>
            <properties>
                <skipUi>true</skipUi>                       <!-- Failsafe -->
                <excluded.test.tags>ui</excluded.test.tags> <!-- Surefire -->
            </properties>
        </profile>
    </profiles>

    <build>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.3.1</version>

                <configuration>
                    <!-- та же argLine -->
                    <!--suppress UnresolvedMavenProperty -->
                    <argLine>${surefire.argLine}</argLine>

                    <!-- запускаем только файлы, содержащие Ui в имени -->
                           <includes>
                               <include>**/*UiIT*</include>   <!-- например RandomFetcherUiIT -->
                           </includes>

                    <!-- по-умолчанию не гоняем; активируем -DincludeUi=true -->
                    <!--suppress UnresolvedMavenProperty -->
                    <skip>${skipUi}</skip>

                    <!-- отдельный JVM на каждый тест, чтобы Swing-трейды не мешали -->
                    <forkCount>1</forkCount>
                    <reuseForks>false</reuseForks>
                </configuration>

                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--  Surefire = unit-tests  -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>

                <configuration>
                    <useModulePath>false</useModulePath>
                    <argLine>
                        --enable-preview
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens java.base/java.util=ALL-UNNAMED
                    </argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                        <arg>--enable-preview</arg>
                        <arg>-Xlint:preview</arg> <!-- показывает детали при необходимости -->
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>org.randomfetcher.App</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version><!-- уже есть -->
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>org.randomfetcher.App</mainClass>
                        </manifest>
                        <manifestEntries>
                            <!-- автофлаг превью + необходимые opens -->
                            <Java-Command>
                                --enable-preview
                                --add-opens=java.base/java.lang=ALL-UNNAMED
                                --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
                                -jar ${project.build.finalName}-jar-with-dependencies.jar
                            </Java-Command>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.4</version>
            </plugin>

            <!--  PIT -->
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>1.16.1</version>

                <dependencies>
                    <!-- JUnit-5 bridge -->
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>1.2.1</version>
                    </dependency>
                </dependencies>

                <configuration>
                    <targetClasses>
                        <param>org.randomfetcher.*</param>
                    </targetClasses>

                    <targetTests>
                        <param>org.randomfetcher.*Tests</param>
                    </targetTests>

                    <excludedTestClasses>
                        <param>org.randomfetcher.RandomFetcherGUITest</param>
                    </excludedTestClasses>

                    <!-- Preview-флаги для JDK 21 -->
                    <jvmArgs>
                        <param>--enable-preview</param>
                        <param>--add-opens</param>
                        <param>java.base/java.lang=ALL-UNNAMED</param>
                        <param>--add-opens</param>
                        <param>java.base/java.lang.reflect=ALL-UNNAMED</param>
                    </jvmArgs>

                    <!-- Временно опустим порог до 60 %,
                         чтобы сборка не падала, пока не добьём тесты -->
                    <mutationThreshold>60</mutationThreshold>
                </configuration>
            </plugin>

        </plugins>
    </build>
</project>
